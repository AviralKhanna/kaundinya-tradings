{
  "name": "kaundinya-tradings-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Lead-capture API for Kaundinya Tradings — stores insurance/SIP enquiries to a file.",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --env-file-if-exists=.env --watch server.js"
  },
  "dependencies": {
    "@vercel/blob": "^2.6.1",
    "@vercel/kv": "^3.0.0",
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
